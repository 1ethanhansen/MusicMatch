from setuptools import setup
from setuptools import find_packages

setup(name='MusicMatch',
      version='0.0.1',
      description='An application to find commonalities in music tastes',
      author='Ethan Hansen',
      packages=find_packages(),
      zip_safe=False)
